# Neovim FHS wrapper for Nix
Use your default `.config/nvim/` normally without worrying about complicated Neovim Nix distros.

## Installing
### Using nix profile

```sh
nix profile add github:faiq1818/nvim-fhs-nix
```
